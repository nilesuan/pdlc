<!--
Template for the user's persistent auto-memory index.

Copy this to ~/.claude/MEMORY.md at install. The live MEMORY.md and the
memory/ directory it points into are gitignored: they hold machine-specific,
often private content (hostnames, account identifiers, resource IDs) and must
never be committed to this repository. Same pattern as settings.example.json.

Each line points to a memory file under memory/ (or a project-scoped memory dir).

Format example (each line is a Markdown link to a real file):

    - [Title]&#40;memory/<file>.md&#41; - one-line hook

Lines after 200 are truncated when loaded into context. Keep entries terse.
Never write memory content into this file directly - write the content to its
own file under memory/ and add the pointer here.
-->

<!-- (no memories yet - entries appear here as you save them) -->
